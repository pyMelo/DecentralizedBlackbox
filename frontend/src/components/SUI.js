// src/components/SUI.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const SUI = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Interfaccia SUI
      </Typography>
      <Typography>
        Qui puoi inserire i contenuti specifici per la piattaforma SUI.
      </Typography>
    </Box>
  );
};

export default SUI;
